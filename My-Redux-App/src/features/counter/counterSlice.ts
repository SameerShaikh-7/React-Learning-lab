import { createSlice } from "@reduxjs/toolkit";

export const counterSlice = createSlice({
    name: "Sameer",
    initialState: {
        counter: 0,

    },
    reducers: {
        // Actions 
        
    }
})